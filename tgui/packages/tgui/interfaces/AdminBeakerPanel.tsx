import { Window } from '../layouts';

export const AdminBeakerPanel = () => {
  return (
    <Window width={1100} height={720}>
      <Window.Content>
        <div />
      </Window.Content>
    </Window>
  );
};
